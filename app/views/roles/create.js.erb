$('#title').val('');
$('#roles').append(
  '<li class="role" id="<%= dom_id @role %>">' + 
    '<p>' +
      '<%= @role.title %>' +
      '<span class="right">' +
        '<%= link_to 'Delete', @role, :method => 'delete', :confirm => 'Are you sure?', :remote => true %>' +
      '</span>' +
    '</p>' +
  '</li>'
);
